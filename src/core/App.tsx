import './App.scss';
import React from 'react';

const App = (): JSX.Element => {
  const title = 'LANARS react school';

  return (
    <div className="App">
      {title}
    </div>
  );
};

export default App;
